<Application x:Class="UserInterface.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!--They sucks (google img), and they are not cc; we need to find cc license art-->
        <ImageBrush  ImageSource="/UserInterface;component/ressources/smallworld_bg.jpg" x:Key="background"/>
        <Image Source="/UserInterface;component/ressources/dwarf_icon.jpg" x:Key="dwarf_icon" Width="32" Height="32"/>
        <Image Source="/UserInterface;component/ressources/orc_icon.gif" x:Key="orc_icon" Width="32" Height="32"/>
        <Image Source="/UserInterface;component/ressources/elf_icon.png" x:Key="elf_icon" Width="32" Height="32"/>

        <Style TargetType="{x:Type RadioButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RadioButton}">
                        <Border x:Name="PART_border" CornerRadius="2" Padding="3px" Margin="2px" Background="Transparent" BorderThickness="1" BorderBrush="{x:Static SystemColors.ControlDarkBrush}" SnapsToDevicePixels="True">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <ContentPresenter/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="PART_border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="{x:Static SystemColors.ControlLightColor}" Offset="0"/>
                                            <GradientStop Color="{x:Static SystemColors.ControlColor}" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
    </Application.Resources>
</Application>
